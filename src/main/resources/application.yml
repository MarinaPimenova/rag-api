#
server:
  servlet:
    context-path: /
    session:
      timeout: 3600 # in seconds, so 60 min  # If a duration suffix is not specified, seconds will be used.
  compression:
    min-response-size: 1024
    mime-types: text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json
    enabled: true
  port: 8080

info:
  app:
    name: rag-api
    version: '@project.version@'

logging:
  level:
    com.epam.training.gen.ai: debug
    org.springframework: info
    org:
      hibernate: info
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      resolve-lazily: false
      enabled: true
  ai:
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: 1536
        initialize-schema: false
        schema-name: public
        table-name: vector_store
        max-document-batch-size: 10000
    openai:
      api-key: ${API_KEY:dummy}
      base-url: ${OPEN_AI_ENDPOINT:http://localhost:11434}
      chat:
        completions-path: ${COMPLETION_PATH:/api/chat}
        model: ${OLLAMA_MODEL:llama3.2}
        base-url: ${OPEN_AI_ENDPOINT:http://localhost:11434}
        embeddings-path: ${EMBEDDINGS_PATH:/api/embeddings}
  datasource:
    url: "jdbc:postgresql://localhost:5432/embeddings_db" #?stringtype=unspecified # 5433
    username: ${POSTGRES_USER:admin}
    password: ${POSTGRES_PASSWORD}
    driverClassName: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        hbm2ddl:
          auto: none
    hibernate:
      ddl-auto: none
    show-sql: true

